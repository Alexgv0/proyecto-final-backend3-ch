import Chai from "chai";
const { expect, assert } = Chai;

describe("", () => {
    it("", () => {
        // Etapa de preparacion
        
        // Etapa de ejecucion

        // Etapa de afirmaciones
    });
    it("", () => {});
    it("", () => {});
    it("", () => {});
    
})